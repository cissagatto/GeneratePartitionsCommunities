% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{hierarchicalPartition}
\alias{hierarchicalPartition}
\title{Perform Hierarchical Partition Analysis}
\usage{
hierarchicalPartition(comm, string1, string2, Save)
}
\arguments{
\item{comm}{A community structure object, typically obtained from hierarchical clustering methods.}

\item{string1}{A string used as a base name for the dendrogram output file (saved as \verb{<string1>-dendro.pdf}).}

\item{string2}{A string used as a base name for the hierarchical partition CSV output file (saved as \verb{<string2>-hierarchical.csv}).}

\item{Save}{A string specifying the directory where output files will be saved.}
}
\value{
A list containing:
\itemize{
\item \code{h_level}: An integer indicating the detected hierarchy level.
\item \code{hybrid.partition}: A string indicating whether hierarchical partitioning was possible (\code{"yes"} or \code{"no"}).
\item \code{partitions}: A dataframe with hierarchical partitions if \code{h_level == 2}, otherwise \code{NULL}.
}
}
\description{
This function evaluates whether a given community structure exhibits a hierarchical organization.
If the structure is hierarchical, it generates a dendrogram visualization and hierarchical partition data.
}
\details{
The function categorizes the hierarchy level as follows:
\itemize{
\item \strong{0}: No merges detected; hierarchical partitioning is not possible.
\item \strong{1}: A single merge detected; hierarchical partitioning is not feasible.
\item \strong{2}: Multiple merges detected; hierarchical structure confirmed.
\item \strong{3}: The structure is not hierarchical.
}

If the community structure is hierarchical (\code{h_level == 2}), the function:
\itemize{
\item Generates and saves a dendrogram plot in a PDF file.
\item Computes hierarchical partitions and saves them as a CSV file.
\item Returns a dataframe containing partition data.
}
}
\examples{
# Example usage:
result <- hierarchicalPartition(comm, "example", "example_output", "./output")
print(result$h_level)

if (!is.null(result$partitions)) {
  print(head(result$partitions))
}

}
