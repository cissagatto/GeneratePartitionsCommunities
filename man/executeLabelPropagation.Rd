% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{executeLabelPropagation}
\alias{executeLabelPropagation}
\title{Execute Label Propagation Community Detection}
\usage{
executeLabelPropagation(parameters)
}
\arguments{
\item{parameters}{A list containing:
\itemize{
\item \code{graph}: An \code{igraph} object representing the graph to be analyzed.
\item \code{weights}: An optional vector of edge weights for the graph.
\item \code{title}: A string used as a prefix for output file names.
\item \code{fold}: A string representing the fold number (for cross-validation scenarios).
\item \code{Save}: The directory path where output files will be saved.
}}
}
\value{
A list containing:
\itemize{
\item \code{original.communities}: A data frame with node community membership information.
\item \code{info.communities}: A data frame with details about the detected communities (e.g., modularity).
\item \code{resulting.partitions}: A list containing hierarchical partitioning results.
}
}
\description{
This function applies the Label Propagation community detection algorithm to a graph, calculates modularity,
and processes the results, including community membership and hierarchical structure. The function also
generates and saves hierarchical partitions, dendrograms, and community structure in the specified output folder.
}
\details{
The Label Propagation algorithm identifies communities by propagating labels through the graph. Each node is assigned
a label initially, and in each iteration, a node adopts the most frequent label of its neighbors. The process stops when
the labels stabilize. Once the algorithm is executed, the results are processed by \code{processCommunityDetection()},
which organizes the data, saves results to files, and generates visualizations.
}
\examples{
parameters <- list(
  graph = my_graph,
  weights = E(my_graph)$weight,
  title = "my_analysis",
  fold = "fold_1",
  Save = "./output"
)
result <- executeLabelPropagation(parameters)
print(result$original.communities)
print(result$info.communities)
print(result$resulting.partitions)
}
\references{
\itemize{
\item Raghavan, U. N., Albert, R., & Kumara, S. (2007). Near linear time algorithm to detect community structures in large-scale networks.
\emph{Physical Review E, 76(3), 036106.}
}
}
