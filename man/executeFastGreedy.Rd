% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{executeFastGreedy}
\alias{executeFastGreedy}
\title{Execute Fast Greedy Community Detection}
\usage{
executeFastGreedy(parameters)
}
\arguments{
\item{parameters}{A list containing:
\itemize{
\item \code{graph}: An \code{igraph} object representing the graph to be analyzed.
\item \code{weights}: An optional vector of edge weights for the graph.
\item \code{title}: A string used as a prefix for output file names.
\item \code{fold}: A string representing the fold number (for cross-validation scenarios).
\item \code{Save}: The directory path where output files will be saved.
}}
}
\value{
A list containing:
\itemize{
\item \code{original.communities}: A data frame with node community membership information.
\item \code{info.communities}: A data frame with details about the detected communities (e.g., modularity).
\item \code{resulting.partitions}: A list containing hierarchical partitioning results.
}
}
\description{
This function applies the Fast Greedy community detection algorithm to a graph,
calculates modularity, and processes the results, including community membership and hierarchical structure.
The function also generates and saves hierarchical partitions and dendrograms in the specified output folder.
}
\details{
The Fast Greedy algorithm detects communities by greedily optimizing modularity
in a hierarchical manner. Once the algorithm is executed,
the results are processed by \code{processCommunityDetection()},
which organizes the data, saves results to files, and generates visualizations.
}
\examples{
parameters <- list(
  graph = my_graph,
  weights = E(my_graph)$weight,
  title = "my_analysis",
  fold = "fold_1",
  Save = "./output"
)
result <- executeFastGreedy(parameters)
print(result$info.communities)
print(result$resulting.partitions)
}
\references{
\itemize{
\item Clauset, A., Newman, M. E., & Moore, C. (2004). Finding community structure in very large networks.
\emph{Physical Review E, 70(6), 066111.}
}
}
