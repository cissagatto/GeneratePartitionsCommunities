% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{processCommunityDetection}
\alias{processCommunityDetection}
\title{Process Community Detection Results}
\usage{
processCommunityDetection(parameters)
}
\arguments{
\item{parameters}{A list containing:
\itemize{
\item \code{community_structure}: The output of a community detection algorithm (e.g., from \code{cluster_walktrap}, \code{cluster_louvain}).
\item \code{method}: A string specifying the name of the community detection method (e.g., "walktrap", "louvain").
\item \code{parameters}: A list of additional parameters for processing (e.g., sparsification settings).
\item \code{graph}: The original graph object used for community detection.
\item \code{title}: A string used as a prefix for output file names.
\item \code{fold}: A string representing the number fold data
\item \code{Save}: The directory path where output files will be saved.
}}
}
\value{
A list containing:
\itemize{
\item \code{original.communities}: A data frame with community membership information.
\item \code{info.communities}: A data frame with details about the detected communities (e.g., modularity, hierarchy).
\item \code{resulting.partitions}: A list containing hierarchical partitioning results.
}
}
\description{
This function processes the results of a community detection algorithm by extracting information
about the detected communities, determining the partition type, and generating hierarchical partitions.
It also saves a dendrogram plot and a CSV file with the partition data.
}
\details{
This function:
\itemize{
\item Extracts the number of communities and determines whether the partition is global, local, or hybrid.
\item Stores community structure and membership information in data frames.
\item Generates hierarchical partitions and saves results to CSV files.
\item Plots and saves the detected community structure.
}
}
\examples{
parameters <- list(
  community_structure = cluster_walktrap(graph),
  method = "walktrap",
  parameters = list(),
  graph = graph,
  title = "my_analysis",
  fold = "fold_1",
  Save = "./output"
)
result <- processCommunityDetection(parameters)
print(result$info.communities)
print(result$resulting.partitions)
}
