% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{executeWalkTrap}
\alias{executeWalkTrap}
\title{Execute Walk Trap Community Detection Algorithm}
\usage{
executeWalkTrap(parameters)
}
\arguments{
\item{parameters}{A list of parameters containing necessary settings for the function.
It must include:
- \code{graph}: A graph object representing the built graph on which the community detection will be performed.
- \code{weights}: A vector of edge weights for the graph (optional).
- \code{title}: A string used as a prefix for output file names.
- \code{fold}: A string representing the fold number (for cross-validation scenarios).
- \code{Save}: The directory path where output files will be saved.}
}
\value{
A list containing:
- \code{original.communities}: A data frame with node community membership information.
- \code{info.communities}: A data frame with details about the detected communities (e.g., modularity).
- \code{resulting.partitions}: A list containing hierarchical partitioning results.
}
\description{
This function applies the Walk Trap community detection algorithm to a graph,
calculates modularity, and stores the results, including community membership and hierarchical structure.
The function also generates and saves the dendrogram, hierarchical partitions, and community structure
in the specified output folder.
}
\details{
The Walk Trap algorithm detects communities based on random walks and is known for identifying
hierarchical community structures.
}
\examples{
parameters <- list(
  graph = my_graph,
  weights = E(my_graph)$weight,
  title = "my_analysis",
  fold = "fold_1",
  Save = "./output"
)
result <- executeWalkTrap(parameters)
print(result$original.communities)
print(result$info.communities)
print(result$resulting.partitions)
}
\references{
\itemize{
\item Pons, P., & Latapy, M. (2006). "Computing communities in large networks using random walks."
\emph{International Symposium on Computer and Information Sciences}, 284-293.
}
}
