% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{executeEdgeBetweenness}
\alias{executeEdgeBetweenness}
\title{Execute Edge Betweenness Community Detection}
\usage{
executeEdgeBetweenness(parameters)
}
\arguments{
\item{parameters}{A list containing:
\itemize{
\item \code{graph}: An \code{igraph} object representing the graph to be analyzed.
\item \code{weights}: An optional vector of edge weights for the graph.
\item \code{title}: A string used as a prefix for output file names.
\item \code{fold}: A string representing the fold number (for cross-validation scenarios).
\item \code{Save}: The directory path where output files will be saved.
}}
}
\value{
A list containing:
\itemize{
\item \code{original.communities}: A data frame with node community membership information.
\item \code{info.communities}: A data frame with details about the detected communities (e.g., modularity).
\item \code{resulting.partitions}: A list containing hierarchical partitioning results.
}
}
\description{
This function applies the Edge Betweenness community detection algorithm to a graph
and processes the results, including modularity, hierarchical structure, and community assignments.
The function also generates and saves hierarchical partitions and dendrograms in the specified output folder.
}
\details{
The Edge Betweenness algorithm detects communities by iteratively removing edges
with the highest betweenness centrality. Once the algorithm is executed,
the results are processed by \code{processCommunityDetection()}, which organizes the data,
saves results to files, and generates visualizations.
}
\examples{
parameters <- list(
  graph = my_graph,
  weights = E(my_graph)$weight,
  title = "my_analysis",
  fold = "fold_1",
  Save = "./output"
)
result <- executeEdgeBetweenness(parameters)
print(result$info.communities)
print(result$resulting.partitions)
}
\references{
\itemize{
\item Girvan, M., & Newman, M. E. (2002). Community structure in social and biological networks.
\emph{Proceedings of the National Academy of Sciences, 99(12), 7821-7826.}
}
}
