% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{executeLeiden}
\alias{executeLeiden}
\title{Execute Leiden Community Detection Algorithm}
\usage{
executeLeiden(parameters)
}
\arguments{
\item{parameters}{A list containing necessary settings for the function.
\itemize{
\item It must include:
\itemize{
\item \code{graph}: A graph object representing the built graph on which the community detection will be performed.
\item \code{weights}: A vector of edge weights for the graph (optional).
\item \code{title}: A string used as a prefix for output file names.
\item \code{fold}: A string representing the fold number (for cross-validation scenarios).
\item \code{Save}: The directory path where output files will be saved.
}
}}
}
\value{
A list containing:
\itemize{
\item \code{original.communities}: A data frame with node community membership information.
\item \code{info.communities}: A data frame with details about the detected communities (e.g., modularity).
\item \code{resulting.partitions}: A list containing hierarchical partitioning results.
}
}
\description{
This function applies the Leiden community detection algorithm to a graph,
calculates modularity, and stores the results, including community membership and hierarchical structure.
The function also generates and saves the dendrogram, hierarchical partitions, and community structure
in the specified output folder.
}
\details{
The Leiden algorithm is an improvement on the Louvain method, designed to find communities more efficiently
and with greater modularity. It works by optimizing the modularity function through local moves and
improving the clustering quality.
}
\examples{
parameters <- list(
  graph = my_graph,
  weights = E(my_graph)$weight,
  title = "my_analysis",
  fold = "fold_1",
  Save = "./output"
)
result <- executeLeiden(parameters)
print(result$original.communities)
print(result$info.communities)
print(result$resulting.partitions)
}
\references{
\itemize{
\item Traag, V. A., Van Dooren, P., & Nesterov, Y. (2019).
"Narrow spectral gaps for community detection".
\emph{Physical Review E, 80(1), 016114.}
}
}
