% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{executeLeadingEigenVector}
\alias{executeLeadingEigenVector}
\title{Execute Leading Eigenvector Community Detection}
\usage{
executeLeadingEigenVector(parameters)
}
\arguments{
\item{parameters}{A list containing:
\itemize{
\item \code{graph}: An \code{igraph} object representing the graph to be analyzed.
\item \code{weights}: A vector of edge weights for the graph (optional).
\item \code{title}: A string used as a prefix for output file names.
\item \code{fold}: A string representing the fold number (for cross-validation scenarios).
\item \code{Save}: The directory path where output files will be saved.
}}
}
\value{
A list containing:
\itemize{
\item \code{original.communities}: A data frame with node community membership information.
\item \code{info.communities}: A data frame with details about the detected communities (e.g., modularity).
\item \code{resulting.partitions}: A list containing hierarchical partitioning results.
}
}
\description{
This function applies the Leading Eigenvector community detection algorithm to a graph,
calculates modularity, and processes the results, including community membership and hierarchical structure.
The function also generates and saves the dendrogram, hierarchical partitions, and community structure
in the specified output folder.
}
\details{
The Leading Eigenvector algorithm identifies community structure by maximizing modularity and
leveraging the eigenvectors of the graph's modularity matrix. Once the algorithm is executed, the results
are processed by the \code{processCommunityDetection()} function, which organizes the data, saves results to files,
and generates visualizations.
}
\examples{
parameters <- list(
  graph = my_graph,
  weights = E(my_graph)$weight,
  title = "my_analysis",
  fold = "fold_1",
  Save = "./output"
)
result <- executeLeadingEigenVector(parameters)
print(result$original.communities)
print(result$info.communities)
print(result$resulting.partitions)
}
\references{
\itemize{
\item Newman, M. E. (2006). Modularity and community structure in networks.
\emph{Proceedings of the National Academy of Sciences, 103(23), 8577-8582.}
}
}
